{
  "name": "rbg-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "concurrently \"cd server && npm i\" \"cd client && npm i\"",
    "start": "concurrently \"nodemon server/server.js\" \"cd client && npm run dev\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
